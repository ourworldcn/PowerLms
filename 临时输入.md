3. 账期管理：反关闭 (Re-open Period)
新需求
配合财务操作习惯（类似金蝶），允许"反结账/反关闭"。即将当前账期倒退一个月（如从 8月退回 7月）。

接口变更
新增接口： 反关闭账期 (Unclose/Reopen)。
逻辑限制： 只能逐月倒退（8月 -> 7月 -> 6月），不可跳跃。
关键参数： isUncloseJobs (Boolean) - 是否同时"解关"该账期下的所有工作号。
如果勾选：工作号恢复为"可编辑"状态。
如果不勾选：仅账期回退，工作号仍保持"关闭/锁死"状态（适用于仅调整财务数据而不动业务数据的场景）。