{
  "EventLog": { // EventSource provider
    "LogLevel": {
      "Default": "Information" // All categories of EventSource provider.
    }
  },
  "DEBUG_SAFE_MODE": false // 设置为 true 可跳过 InitializerService 启动，避免 VS2022 调试异常
}
