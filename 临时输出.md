# 📋 本周服务器端修改计划（2026-03-15 ~ 2026-03-21）

> **信息来源**：临时输入.md（2026-03-14会议纪要）+ 会议纪要.md（历史记录）+ TODO.md + 代码实际状态验证
>
> **项目**：PowerLmsServer.sln（.NET 6）
>
> **角色**：ZC@WorkGroup（后端）

---

## 1 📊 任务总览（按优先级排序）

| 序号 | 任务 | 来源 | 优先级 | 预估工时 | 前置依赖 | 阻塞状态 |
|------|------|------|--------|----------|----------|----------|
| T1 | 通用报表模板系统 - 实体+CRUD | 临时输入§6 | 🔴第一 | 1.5天 | 无 | ✅可开始 |
| T2 | 空运接口架构整理 - 消除EaDoc重复 | 会议纪要§4.1 | 🔴第一 | 0.5天 | 无 | ✅可开始 |
| T3 | 费用过滤Bug联调确认 | 会议纪要§4.2 | 🟡第二 | 0.5天 | 前端联调 | ⚠️需联调 |
| T4 | 报关专用字典7个新表 | TODO§2.1 | 🟡第二 | 0.5天 | 数据结构待确认 | ⚠️部分阻塞 |
| T5 | 报关单字段整理与补充 | 临时输入§3 | 🟢第三 | 1天 | 界面原型待确认 | ⚠️部分阻塞 |
| T6 | 海运提单数据查询API增强 | 临时输入§2+§4 | 🟢第三 | 0.5天 | 背景图/字段确认 | ⚠️部分阻塞 |

---

## 2 🔴 第一优先级（本周必须完成）

### 2.1 T1：通用报表模板系统 - 后端实体与CRUD接口

**来源**：临时输入.md §6（通用报表模板系统设计）

**当前代码状态**：❌ 完全不存在，需从零创建

**需求摘要**：
- 实体核心字段：ID/GUID、JSON模板定义（不可见）、报表类别、报表名称、（可选）组织ID、模板类型
- 背景图存储：URL方式（推荐），复用通用上传接口
- CRUD权限：仅商管/超管可创建/修改/删除，普通用户可查询/运行
- 模板JSON对终端用户不可见，前端解析渲染

**后端实施步骤**：
1. **创建实体类** `PowerLmsData/报表/PlReportTemplate.cs`
   - 继承 `GuidKeyObjectBase`（与项目其他实体一致）
   - 字段：`Name`(报表名称)、`Category`(报表类别)、`TemplateKind`(模板类型)、`JsonContent`(JSON模板定义)、`BackgroundImageUrl`(背景图URL)、`OrgId`(组织ID)、`Remark`(备注)
   - 标准审计字段：`CreateDateTime`、`CreateBy` 等
2. **注册DbSet** `PowerLmsData/PowerLmsUserDbContext.cs` 中添加 `DbSet<PlReportTemplate>`
3. **创建Controller** `PowerLmsWebApi/Controllers/Report/ReportTemplateController.cs`
   - 标准CRUD：查询(GetAll)、新增(Add)、修改(Modify)、删除(Delete)
   - 权限控制：CRUD操作需校验商管/超管角色
4. **创建DTO** `PowerLmsWebApi/Controllers/Report/ReportTemplateController.Dto.cs`
5. ⚠️ **不生成数据库迁移**（手工执行）

**待确认（不阻塞骨架实现）**：
- 模板类型枚举值（可先用字符串，后续补枚举）
- 模板与业务的绑定策略（模板类型关键字与组织/业务ID的校验规则）

---

### 2.2 T2：空运接口架构整理 - 消除EaDoc CRUD重复

**来源**：会议纪要§4.1（空运接口丢失 → 架构问题）

**当前代码状态**（已验证）：
- ✅ `PlAirborneController.cs`：包含空运进口(IaDoc) + 空运出口(EaDoc) 的完整CRUD（含日志、错误处理、权限校验）
- ⚠️ `PlJobController.EaDoc.cs`：包含**重复的**空运出口(EaDoc) CRUD（较简陋，无日志无详细错误处理）

**实施步骤**：
1. 对 `PlJobController.EaDoc.cs` 中的 EaDoc 相关方法添加 `[Obsolete]` 标记，指向 PlAirborneController
2. 与前端(陈云霄)确认是否已迁移到 PlAirborneController 的接口
3. 确认后删除 `PlJobController.EaDoc.cs` 中的 EaDoc CRUD 方法（保留 EaDocItem 子表方法如有）
4. 编译验证

**风险评估**：
- 前端可能仍在调用 PlJobController 下的 EaDoc 接口，需确认后再删除
- 建议先标 Obsolete + 日志警告 → 观察一周 → 再删除

---

## 3 🟡 第二优先级（本周尽量完成）

### 3.1 T3：费用反查申请单明细过滤Bug联调

**来源**：会议纪要§4.2（fee_id参数过滤未生效）

**当前代码状态**（已验证）：
- ✅ `DocFeeRequisitionManager.GetAllDocFeeRequisitionItemQuery()` 已实现完整的多表过滤逻辑
- 支持 `DocFeeRequisitionItem.FeeId=xxx` 格式的条件过滤
- 过滤通过 `QueryHelper.GenerateWhereAnd()` 通用方法实现
- `FinancialController.GetDocFeeRequisitionItem()` 正确调用了该方法

**分析结论**：后端过滤逻辑**代码层面正确**，问题可能在于：
1. 前端传参格式不正确（应传 `DocFeeRequisitionItem.FeeId=xxx` 而非 `fee_id=xxx`）
2. `QueryHelper.GenerateWhereAnd()` 对 Guid 类型字段的匹配可能有问题

**实施步骤**：
1. 验证 `QueryHelper.GenerateWhereAnd()` 对 `FeeId`（Nullable Guid）字段的过滤是否正确
2. 与前端联调确认传参格式
3. 如确认为后端问题，修复过滤逻辑

---

### 3.2 T4：报关专用字典7个新表 - 实体骨架创建

**来源**：TODO.md §2.1（报关模块专用字典表）

**当前代码状态**（已验证）：
- ✅ 已存在6个报关字典：`CdHsCode`、`CdGoodsVsCiqCode`、`CdPlace`、`CdDomesticPort`、`CdInspectionPlace`、`CdPort`
- ✅ 已有CRUD控制器 `CustomsDictionaryController.cs`（6组CRUD）
- ✅ 已有导入导出支持 `CustomsDictionaryController.ImportExport.cs`
- ❌ TODO中列出的7个**新**字典表（CdDealKind等）**尚未创建**
- ⚠️ TODO声称"6个实体已创建"指的是已有报关字典，不是新的7个

**待创建字典清单**：

| 实体名 | 中文名 | 基类 |
|--------|--------|------|
| CdDealKind | 成交方式 | NamedSpecialDataDicBase |
| CdTaxExemptionKind | 征免性质 | NamedSpecialDataDicBase |
| CdTaxCollectionKind | 征免方式 | NamedSpecialDataDicBase |
| CdTransportKind | 运输方式 | NamedSpecialDataDicBase |
| CdSupervisionKind | 监管方式 | NamedSpecialDataDicBase |
| CdCustomsPort | 报关专用港口 | NamedSpecialDataDicBase |
| CdTransportTool | 运输工具 | NamedSpecialDataDicBase |

**实施步骤**（先创建骨架，待数据结构确认后补字段）：
1. 在 `PowerLmsData/报关/CustomsDictionaries.cs` 中追加7个实体类
2. 在 `PowerLmsUserDbContext.cs` 中添加7个 DbSet
3. 在 `CustomsDictionaryController.cs` 中添加7组CRUD操作（参考现有CdHsCode）
4. 在 `CustomsDictionaryController.Dto.cs` 中添加对应DTO
5. 在 `ImportExportService.cs` 的 `IsIndependentDictionaryEntity()` 和导入导出逻辑中注册新实体
6. ⚠️ **不生成数据库迁移**

**阻塞项**：永昌石需在周一前提供详细表结构和数据

---

## 4 🟢 第三优先级（本周有余力则推进）

### 4.1 T5：报关单字段整理与补充

**来源**：临时输入.md §3（报关单界面与字段整理）

**当前代码状态**：
- ✅ `CustomsDeclaration.cs`（报关单主表实体）已存在
- ✅ `CustomsDeclarationController.cs` 已存在
- 需要按海关界面字段顺序整理字段
- 主表分三块：报关基本资料、电子口岸、报检基本数据

**阻塞项**：
- 字段顺序需按海关界面确认（示例编号33/39/55/65/118/131）
- 橙色标注的新增字段需确认（会议提到仅一个新增）
- 需要界面原型确认后再整理字段

**本周可推进**：
1. 整理现有 `CustomsDeclaration` 实体字段清单并与海关编号对应
2. 确认已有字段覆盖率
3. 待原型确认后补充缺失字段

---

### 4.2 T6：海运提单数据查询API增强

**来源**：临时输入.md §2+§4（海运提单套打、主/分单数据模型）

**当前代码状态**：
- ✅ 海运出口实体已存在：`PlEsDoc`、`EsMbl`（主提单）、`EsHbl`（分提单）
- ✅ 海运进口实体已存在：`PlIsDoc`
- 套打报表渲染为前端任务，后端需确保数据查询API完善

**关键讨论点**：
- 分单字段统一命名为 HBL label，主单为 MBL label（命名规范调整）
- 分单必须隶属于主单（业务约束），报表渲染时从主单取值
- 当前系统以 Job Number 关联，被指出"关联不太可靠"

**本周可推进**：
1. 检查 EsMbl/EsHbl 的现有API是否满足套打报表取数需求
2. 如缺少关联查询接口（如通过主单号获取所有分单），补充查询API

**阻塞项**：
- 空单背景图与尺寸参数（待对方提供）
- 费用支付字段取值范围/业务口径待确认

---

## 5 ⏸️ 暂缓/不在本周范围的任务

| 任务 | 原因 |
|------|------|
| 结算单导出到金蝶 | 会议纪要§4.3决议暂缓 |
| 工资代发平台税务功能 | 临时输入§1，非代码任务，属于运维/财务策略 |
| 开航日期与运价参考浮动窗口 | 临时输入§5，主要是前端UI方案，后端运价数据API已有 |
| 客户选择器优化 | 会议纪要§5.1，前端任务 |
| 付款单界面调整 | 会议纪要§5.1，前端任务 |
| 菜单调整 | 会议纪要§5.1，前端任务 |
| SSL证书替换 | TODO§1.1，运维操作 |
| 部署最新版本 | TODO§1.2，运维操作 |

---

## 6 📝 关键发现与风险提示

### 6.1 ⚠️ TODO.md数据不准确
- TODO.md §2.1.5 声称"6个实体类已创建"指的是**已有的**报关字典（CdHsCode等），**不是**待创建的7个新字典（CdDealKind等）
- 7个新字典实体在当前代码中**完全不存在**，需要从零创建
- 建议本周完成后更正TODO.md状态

### 6.2 ⚠️ 费用过滤Bug可能非后端问题
- `GetAllDocFeeRequisitionItemQuery` 过滤逻辑代码层面正确
- 使用 `conditional` 字典 + `QueryHelper.GenerateWhereAnd()` 通用过滤
- 前端需传 `DocFeeRequisitionItem.FeeId=<Guid>` 格式，而非 `fee_id=<Guid>`
- 建议先与前端联调确认传参格式

### 6.3 ⚠️ 报表模板系统设计决策
- 临时输入中提到"先不复杂化权限"，建议实体先不加权限字段
- 背景图存储选择URL方式，需确认通用上传接口的路径策略（历史曾有路径/权限导致图片不可见的问题）
- 模板类型先用字符串字段，后续可根据业务需要改为枚举

---

## 7 📅 本周建议日程

| 日期 | 任务 | 交付物 |
|------|------|--------|
| 周一 | T1：报表模板实体+DbSet+Controller骨架 | 编译通过的实体和CRUD接口 |
| 周二 | T1：报表模板CRUD完善 + T2：空运接口Obsolete标记 | 完整的报表模板API + 空运接口标记 |
| 周三 | T4：报关新字典实体骨架（如数据结构已确认） | 7个字典实体+DbSet+CRUD |
| 周四 | T3：费用过滤Bug联调 + T4：字典导入导出注册 | Bug修复或确认非后端问题 |
| 周五 | T5/T6：报关单字段/海运提单API（视阻塞项解除情况） | 按需推进 |

---

*本计划基于2026-03-15代码状态验证，如有变更请及时更新。*
